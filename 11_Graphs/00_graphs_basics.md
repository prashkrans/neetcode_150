# Graphs:

### Important learnings:
1. Whether a graph is cyclic.
    a. Undirected:
        a. DFS + Parent variable
        b. BFS + Parent array
    
    b. Directed:
        a. DFS + recStack(using a vector) + backtrack
        b. Kahn's Algorithm